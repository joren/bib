<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style>
      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .button {
        display: inline-block;
        padding: 12px 24px;
        background-color: #2563eb;
        color: #ffffff;
        text-decoration: none;
        border-radius: 6px;
        font-weight: 600;
        margin: 20px 0;
      }
      .footer {
        margin-top: 30px;
        padding-top: 20px;
        border-top: 1px solid #e5e7eb;
        color: #6b7280;
        font-size: 14px;
      }
    </style>
  </head>
  <body>
    <h2>Sign in to Bib</h2>

    <p>Hi <%= @user.name || @user.email %>,</p>

    <p>Click the link below to sign in to your family library:</p>

    <p>
      <%= link_to "Sign in to Bib", @login_url, class: "button" %>
    </p>

    <p>Or copy and paste this URL into your browser:</p>
    <p><%= @login_url %></p>

    <div class="footer">
      <p>This link will expire at <%= @expires_at.strftime("%B %d, %Y at %I:%M %p") %>.</p>
      <p>If you didn't request this, you can safely ignore this email.</p>
    </div>
  </body>
</html>
